apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cipherradar

resources:
  - namespace.yaml
  - configmap.yaml
  - api-deployment.yaml
  - frontend-deployment.yaml
  - scanner-worker-deployment.yaml
  - redis-deployment.yaml
  - services.yaml

commonLabels:
  app.kubernetes.io/part-of: cipherradar
