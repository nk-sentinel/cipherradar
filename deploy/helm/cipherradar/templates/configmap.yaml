apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cipherradar.fullname" . }}
  namespace: {{ include "cipherradar.namespace" . }}
  labels:
    {{- include "cipherradar.labels" . | nindent 4 }}
data:
  CBOM_REDIS_URL: {{ include "cipherradar.redisUrl" . | quote }}
  CBOM_DATABASE_URL: {{ include "cipherradar.databaseUrl" . | quote }}
  {{- if .Values.externalS3.endpoint }}
  CBOM_S3_ENDPOINT: {{ .Values.externalS3.endpoint | quote }}
  {{- end }}
  {{- if .Values.externalS3.bucket }}
  CBOM_S3_BUCKET: {{ .Values.externalS3.bucket | quote }}
  {{- end }}
