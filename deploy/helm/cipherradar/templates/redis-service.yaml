{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cipherradar.fullname" . }}-redis
  namespace: {{ include "cipherradar.namespace" . }}
  labels:
    {{- include "cipherradar.redis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "cipherradar.redis.selectorLabels" . | nindent 4 }}
{{- end }}
