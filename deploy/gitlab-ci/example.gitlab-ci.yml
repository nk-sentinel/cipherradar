# Example: Add to your project's .gitlab-ci.yml
include:
  - local: 'deploy/gitlab-ci/cradar-scan.gitlab-ci.yml'

# Override variables as needed
variables:
  CRADAR_PASSES: "1"
  # CRADAR_POLICY: "policy.cradar.yml"
  # CRADAR_FAIL_ON: "high"
